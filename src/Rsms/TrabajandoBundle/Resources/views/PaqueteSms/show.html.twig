{% extends '::base.html.twig' %}
{% block menuLateral %}  
    <div class="span3">
        <div class="well sidebar-nav">
            <ul class="nav nav-list">
                <li class="nav-header">Paquetes Sms</li>
                <li class="active"><a href="{{ path('paquetesms_show', { 'id': entity.id }) }}">Ver</a></li>                            
                <li class=""><a href="{{  path('paquetesms_edit', { 'id': entity.id }) }}">Editar</a></li> 
                <li class=""><a href="{{ path ('paquetesms') }}">Listar</a></li>   
            </ul>
        </div><!--/.well -->
    </div><!--/span-->
{% endblock %}
{% block body -%}
    <h1>Paquetes Sms</h1>

    <table class="table table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.nombre }}</td>
            </tr>
            <tr>
                <th>Cantidadsms</th>
                <td>{{ entity.cantidadSms }}</td>
            </tr>
        </tbody>
    </table>

    <ul class="record_actions">
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
