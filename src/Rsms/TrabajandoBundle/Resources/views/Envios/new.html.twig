{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Envios creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('envios') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
