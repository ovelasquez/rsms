{% extends '::base.html.twig' %}

{% block menuActI %}{% endblock %}
{% block menuActCPS %}class="active"{% endblock %}
{% block menuActP %}{% endblock %}

{% block menuLateral %}  
    <div class="span3">
        <div class="well sidebar-nav">
            <ul class="nav nav-list">
                <li class="nav-header">Crédito</li>
                <li class="active"><a href="{{ path ('clientepaquetesms') }}">Listar</a></li> 
                <li class=""><a href="{{ path ('clientepaquetesms_new') }}">Crear</a></li>              
            </ul>
        </div><!--/.well -->
    </div><!--/span-->
{% endblock %}
{% block body -%}
    <h1>Clientes Paquete Sms </h1>

    <table class="table table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cliente</th>
                <th>Paquete Sms</th>
                <th>Fecha</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for entity in entities %}
                <tr>
                    <td><a href="{{ path('clientepaquetesms_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                    <td>{{ entity.cliente }}</td>
                    <td>{{ entity.paquetesms }}</td>
                    <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>                                        
                    <td>
                        <ul>
                            <li>
                                <a href="{{ path('clientepaquetesms_show', { 'id': entity.id }) }}">show</a>
                            </li>
                            <li>
                                <a href="{{ path('clientepaquetesms_edit', { 'id': entity.id }) }}">edit</a>
                            </li>
                        </ul>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>       
{% endblock %}